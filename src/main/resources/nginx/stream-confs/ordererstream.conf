

map $ssl_preread_server_name $targetOrdererBackend {
  
}

server {
  listen 443;

  proxy_connect_timeout 1s;
  proxy_timeout 3s;
  # resolver 1.1.1.1;

  proxy_pass $targetOrdererBackend;
  ssl_preread on;
}
